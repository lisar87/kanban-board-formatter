{
  "name": "kanban-board-formatter",
  "version": "0.1.0",
  "description": "Parses and normalizes plain-text kanban board exports, with precise line/column error messages.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "kanban-format": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
